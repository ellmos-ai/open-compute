# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/

# Database files
*.db

# IDE
.idea/
.vscode/

# Data directories
data/

# Virtual environments (never inside cloud-synced dirs)
.venv/
venv/
env/

# Environment / secrets
.env
*.key
*.pem

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Local session files and internal reports (not for publish)
_session/
_reports/
RELEASE_GATE.md

# Local learning / push state (never commit; gitignored by design)
_state/

# Archived throwaway/foreign scratch (kept, not published)
_archive/

# Multi-Agent Lock-System (lokal, nie committen)
LOCK
LOCK.*
LOCK*.txt
LOCK.permissions.json
*.lock
uv.lock
!package-lock.json

# Multi-Host Sync-Konflikte & Temporärdateien
* (kopie)*
* (copy)*
*-WORKSTATION*
*-WORKSTATION-LG*
*-ASUS-GEI*
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*-conflict-*
*.sync-temp-*
*.orig

# Testing, Linting & Coverage Caches
.ruff_cache/
.coverage
.coverage.*
coverage/
htmlcov/
.mypy_cache/
.tox/
.turbo/
.nyc_output/
wheelhouse/
.wheel-smoke/

# Temporäre Dateien & Backups
*.tmp
*.bak
*.swp
*~
*.log

# Pflegelauf-Protokolle (Pipeline-Material, kein Repo-Inhalt)
_after-care/

# SDD-Prozess-Artefakte (lokal)
.superpowers/

# Interner Planungsplan (lokal, nicht für Publish)
MCP-PLAN.md
